import React from "react";
import "./Header.css";

class Header extends React.Component {
  render() {
    return (
      <header>
        <span className="logo">My Weather</span>
      </header>
    );
  }
}

export default Header;
